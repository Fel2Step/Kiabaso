# ── Base de Dados (TiDB / Railway / MySQL) ─────────
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=kiabasso

# ── JWT ────────────────────────────────────────────
JWT_SECRET=gerar_hash_256_bits_aleatorio_aqui
JWT_REFRESH_SECRET=outro_hash_256_bits_aleatorio_aqui
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ── Email (SMTP - opcional) ────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# ── SMS (Twilio - opcional) ────────────────────────
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# ── App ────────────────────────────────────────────
PORT=5000
NODE_ENV=production
FRONTEND_URL=https://teu-projeto.vercel.app
